# v2/data/

CSVs written by `LabJackRecordData[]`/`LabJackTempSweep[]` and read by `LabJackPlotData[]` (see
[../docs/API_REFERENCE.md](../docs/API_REFERENCE.md)). This folder is tracked in git, but the
recorded CSVs it fills up with are not (see `.gitignore`) -- they're experiment output, not source.